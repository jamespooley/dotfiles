\set QUIET 1
\pset null 'Ø'

\timing

\set VERBOSITY verbose
\set HISTFILE ~/.psql_history- :DBNAME
\set HISTCONTROL ignoredups

-- Autocomplete keywords in upper case regardless of typed case
\set COMP_KEYWORD_CASE upper
\unset QUIET

\set PROMPT1 '%M:%> %n@%/%R%#%x '
